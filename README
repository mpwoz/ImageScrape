A simple image scraper.

*NOTE: to run this, you'll need all the required gems.
	Install these by running 'bundle install' 

	Alternatively, run 'bundle install --local' to install a local copy of gems.


